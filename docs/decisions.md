# 設計判断ログ

## 2026-08-10
- 実行環境（ヘッドレスコンテナ、Playwright+Chromium あり、Unity/Unreal 不可）を踏まえ、ゲームはブラウザベース（HTML5/Canvas/JS）で開発する方針とした。
- セッション間で会話コンテキストは共有されないため、引き継ぎ情報はすべてリポジトリ内のファイル（CLAUDE.md / docs/）に残す運用とした。
- AI_project001 は現時点でこのセッションの GitHub 認証からアクセス不可。参照が必要な場合はアクセス権付与が必要。

- 販路戦略: 収益は CrazyGames（広告収益60%・非独占）、日本向け露出は PLiCy / GitHub Pages / X。unityroom は Unity/Godot 限定のため Phaser 製は投稿不可。日本には収益分配型ポータルがほぼ無い（アツマール終了済み）。
- ゲームは日英2言語対応（navigator.language で自動判定＋手動切替）。CrazyGames SDK はラッパー経由で、SDK が無い環境では no-op。

## 2026-08-11
- 転生システム: 累計100万調合で解放、エッセンス = floor(sqrt(累計/100万))、1個につき生産・クリック+10%（永続）。平方根スケールで転生を繰り返すほど周回が速くなる標準設計。
- GitHub Pages デプロイを GitHub Actions で自動化（main への push で typecheck→test→build→deploy）。vite base は './'。リポジトリ設定で Pages のソースを「GitHub Actions」にする必要あり（人間の作業）。

## 2026-08-12
- 実績システムは lifetimeBrewed/totalClicks/unlockedAchievements を転生時にも保持するフィールドとして追加（totalBrewed/potions/counts は転生でリセット、実績関連は永続）。
- クリック強化は既存の clickPower フィールドを購入のたびに+1する方式（コストは指数増加、baseCost 50 / growth 1.6）。新フィールドを増やさずシンプルに実装。
- サウンドは外部音源を使わず Web Audio API でその場合成（アセット0・軽量・著作権フリー）。AudioContext はユーザー操作後にのみ開始できるため遅延初期化。
- モバイル対応は Phaser の Scale.FIT + CENTER_BOTH のみで対応（タッチはPhaserのpointerイベントがマウス/タッチを統一的に扱うため追加実装不要）。ネイティブアプリ化はせず、あくまでブラウザのレスポンシブ対応に留める。
- typescript-eslint が TypeScript 7.x に未対応（2026-08時点、upstream issue #10940）のため、eslint実行時のみ typescript を 6.0.3 に固定。tsc本体（tsconfig経由のtypecheck）は引き続きプロジェクトのtypescriptバージョンに従う。
- OGP画像はPillow等の画像ライブラリが無い環境のため、Pythonのzlib/structで直接PNGバイトを生成した単色グラデーションのプレースホルダー。投稿前に差し替え推奨（docs/submission.mdに記載）。
- バンドルサイズ警告はPhaser本体が1.3MB超のため恒久的に発生する。manualChunksでアプリコードと分離してキャッシュ効率を上げた上で、chunkSizeWarningLimitを実態に合わせて調整（警告を黙らせるのではなく閾値を正しくする方針）。

## 2026-08-22
- 2本目のゲーム「剣戟の森」（横スクロールアクション、剣で敵を倒す）を追加。同一リポジトリ内 `games/side-scroller/` に完全独立の Phaser+TS+Vite プロジェクトとして配置（ルートのポーション工房とは node_modules/設定を共有しない）。既存の公開URL（ルート = ポーション工房）を壊さないため。
- 戦闘ロジック（ダメージ計算、無敵時間、攻撃クールダウン、射程判定、ゴール判定）は `src/logic/combat.ts` に純粋関数として分離し Vitest で単体テスト（19件）。Phaser 側の GameScene は物理演算・入力・描画のみを担当し、実際のダメージ判定はロジック層の関数呼び出しに委譲する設計は1本目のポーション工房と同じ方針を踏襲。
- スプライトは画像アセットを使わず Phaser の Graphics.generateTexture() でその場生成した単色矩形を使用（アセット0）。
- デプロイワークフローを拡張し、ルートと `games/side-scroller/` を両方ビルドして `dist/side-scroller/` にマージしてから Pages にアップロード。同じ GitHub Pages サイト配下に `/` と `/side-scroller/` として共存させる。
- 動作確認で「攻撃が敵に当たらない」ように見えた事象は、Playwright の `keyboard.press()` が非常に短い合成イベントで Phaser の JustDown 判定を取りこぼすケースがあったことと、敵の徘徊AIによりプレイヤーの向きに対して射程外に出ていたことが原因で、戦闘ロジック自体にバグは無かった（`keyboard.down()`/`up()` を分けて呼ぶテストで撃破・スコア加算まで確認済み）。

## 2026-08-24
- ポーション工房をリポジトリ直下から `games/potion-workshop/` に移動し、剣戟の森と構成を統一（両方とも `games/<name>/` の独立プロジェクト）。
- リポジトリ直下は Node プロジェクトをやめ、静的なゲーム一覧ハブページ（`index.html` 1枚、ビルド不要）に変更。旧ルートURL（ポーション工房が直接表示）は `/potion-workshop/` に変わる（今回は既存URLの互換維持よりも構成の一貫性を優先）。
- deploy.yml は両ゲームをビルドしてそれぞれ `/potion-workshop/`・`/side-scroller/` サブパスに配置し、ハブページと合わせて1つの dist にまとめてから Pages にアップロードする方式に変更。

- ポーション工房も `games/potion-workshop/` に移動し、side-scroller と構成を統一。リポジトリ直下は静的なゲーム一覧ハブページ（ビルド不要の index.html 1枚）に変更。既存の公開URLは `/potion-workshop/` サブパスに変わる（ユーザー了承済み）。
- deploy.yml の setup-node で `cache: npm` がリポジトリ直下の package-lock.json を探して失敗する事象が発生（直下がNodeプロジェクトでなくなったため）。`cache-dependency-path` で両ゲームのlockfileを明示指定して解消。
- 剣戟の森「当たり判定が動いていない」報告の調査: 原因はプレイヤーと敵の接触が overlap のみ（衝突判定 collider が無い）だったため、敵の体をすり抜けてしまい剣の間合いに留まれないことだった。collider に変更し、物理的にぶつかって自然に隣接するよう修正。加えて攻撃レンジを46→60px、背後方向に12pxの許容を追加、縦方向の判定ガードも追加。デバッグはPlaywrightで実際のプレイ操作をシミュレートし、プレイヤー座標・敵座標・判定結果をconsole.logで可視化して特定した（本番コードにはデバッグログを残していない）。
- 剣戟の森のキャラクター表現を単色矩形から、頭・胴・剣を持つ簡易ヒューマノイド（Phaser Graphics で生成、画像アセットなし）に強化。HUDにパネル背景、リザルト画面にパネル演出、被弾/命中時のフィードバック（フラッシュ・ノックバック・カメラシェイク）を追加。GAME OVER/CLEAR 後は R キーでシーン再スタート可能に。

## 2026-08-24
- ポーション工房: クリック強化を1回ずつではなく x1/x5/x10/x100/MAX の一括購入に対応。合計コストは等比級数の和で計算し、資金が足りない場合は「買えるだけ買う」部分約定方式（0円で失敗にはしない）。
- ポーション工房: オフライン進行上限を基本8h→12hに変更し、72hまで拡張できる仕組みを追加。実装は「ソースIDをキーにした加算式ボーナスの合計を基本値に足し、絶対上限でクランプする」設計（`offlineCapBonuses: Record<string, number>`）にした。これにより課金・強化・上限突破・バフ装備など性質の異なる複数の拡張手段を、互いに干渉せず後から自由に追加できる。現時点で具体的に実装したのは essence 消費の購入型拡張（1レベル+6h、最大10レベルでちょうど72hに到達）のみ。他のソースは `setOfflineCapBonus`/`addOfflineCapBonus` を呼ぶだけで追加可能。

- 剣戟の森: 武器種別（近接/中距離/遠距離）、スキル、奥義、秘奥義を追加。武器はWeaponDefとしてrange/damage/cooldownMs/attackWindowMsを持たせ、通常攻撃のcanAttack/startAttackは装備中武器のパラメータを参照するよう変更（デフォルトのmelee武器の数値は既存定数と完全一致させたため、既存20件のテストは無修正で通過）。
- コマンド入力（格闘ゲーム風）は`src/logic/commandInput.ts`に完全に独立した純粋関数として実装。トークン列（down/forward/back/attack）をタイムスタンプ付きでバッファに積み、末尾がシーケンスと一致し、かつ各トークン間隔が規定時間内かを判定する方式。forward/backは画面上の左右キーではなく「プレイヤーの向きに対して相対」で解釈する（格闘ゲームのコマンドは常に自キャラ前方基準のため）。
- 秘奥義の解放条件は「今の周回のスコアが500に到達（＝敵5体撃破相当）」とした。ユーザーからは「特殊な条件下で」とだけ指定があり具体的な閾値の指定は無かったため、調整しやすいよう`HIOUGI_UNLOCK_SCORE`という単一定数に集約している。
- 奥義/秘奥義発動中は「持続的な範囲攻撃」として実装（isOugiActive/isHiougiActiveがtrueの間、毎フレーム範囲内の敵にダメージ判定）。単発の大ダメージ一撃ではなく短時間の広範囲薙ぎ払いにしたのは、複数の敵を同時に巻き込める爽快感を優先したため。

## 2026-08-24（続き）
- ルートハブページ（`index.html`）をリッチなカードUIに刷新。データ駆動の配列を`<script>`内に定義しJSでカードを描画（ビルド不要の方針を維持）。他リポジトリの制作物は`external: true`＋フルURLで追加できる「リンク集」方式とし、ビルド時の取り込みは行わない（ユーザーの選択: リンク集として扱う／ゲームサムネ風のリッチなカードUI）。
- ハブページは当初「ゲーム一覧」という文言で作成したが、ユーザーから「ゲームだけでなく色んなアプリも載せる想定」との指摘を受け、タイトル・見出し・配列名（`GAMES`→`PROJECTS`）・タグ語彙をゲーム限定を前提としない表現に修正。今後Webアプリ等を追加する際もこの前提に沿うこと。
- ユーザーから見た目の参考として https://amix-design.com/tl/web-g-games/ の共有あり（「作り込みすぎる必要はない」とのコメント付き）。本セッションのサンドボックスは外部URLにアクセスできないため詳細は反映できていない。次回以降デザインを見直す際の参考として記録。

## 2026-08-24（剣戟の森: 武器パラメータ体系の設計合意・未実装）

ユーザーと議論の上、以下の設計で合意した。**まだ実装には着手していない**（対話での合意事項のみ）。次にこのテーマに着手するセッションは、下記を踏まえて型定義から実装すること。

### データモデル階層
```
WeaponKind: melee / mid / ranged（既存3種、装備スロットに対応。変更なし）

WeaponTemplate（武器の原型。例: 「鉄の剣」「戦槍」「大剣」。同一kind内に複数用意）
  └ WeaponInstance（ロードアウト用の個体。レア度＋3種の成長軸を持つ）
      アウトゲームで近距離/中距離/遠距離の各スロットに1本ずつ設定しておく（ロードアウト）

BaseEquipment（基本装備。kindごとに固定・弱い。ステージ開始時は常にこれからスタート。
  レベルアップ可能だがロードアウト武器には遠く及ばない上限に抑える。
  「弱い状態から強くなる」体験を守るのが目的で、縛りプレイでも常にこの水準が前提）

SummonMedium（召喚媒体。インゲーム内で入手するアイテム、種類は問わず共通の1系統）
  → 拾うと一時停止し、ヴァンサバ風の選択UIでロードアウト済みの武器/防具/アイテムから
    呼び出す対象を選ぶ（ステージ側から提示される一時バフ等は別枠のプールとして
    後日設計する。今回はアウトゲーム設定物の召喚のみが対象）
  → 選択すると、空中に武器が出現してプレイヤーがパシッと受け取る演出を入れる（要実装）
  → 一度呼び出した武器はそのステージ中ずっと保持（魔界村のような被弾ロストはしない）
  → 同じ武器の媒体を再度拾うと「インゲーム限定の重複強化」が入る（ヴァンサバ方式）。
    アウトゲームの強化/進化/熟練度とは完全に独立したrunLevel的なカウンタで管理する

Armor（防具）: 消耗品。被弾ごとに耐久が減り、0で消滅
Item（アイテム）: 消耗品。使用したら1回で消滅
```

### レア度（Rarity）
- Tierごとに合計ステータス（距離/パワー/速さ/範囲/重さ/基本コンボ数）のバジェット範囲を設定し、個体生成時にその範囲内で配分をロールする
- 「同格なら距離が長い方が有利」という現実的な傾向は保ちつつ、レア度が上がれば距離以外のパラメータを伸ばして総合力で上回れるようにする（距離一強にしない）
- 個体の成長軸は3種、独立して管理: 強化（素材/通貨消費で段階的にレベルアップ）／進化（強化上限到達後に発展、上限自体を引き上げ）／熟練度（使用実績で自動成長、主にコンボ数への加算源）
- 入手経路: 宝箱（確率抽選、レア度が上がるほど排出率低下）／鍛治（テンプレート＋目標レア度を指定して確定入手）

### インゲーム内重複強化（ヴァンサバ/ダダサバ形式）
- 1回の召喚につき最大4段階: 「入手」→「強化×2回」→「アルティメット化×1回」
- アルティメット化はレア度SR以上の武器のみ可能
- 重複強化で伸ばすのは主に「パワー」「速さ」の2軸（マスト）。強化段階が進むほどコンボ数も増える、といった幅は持たせる（強化しすぎると全パラメータが伸びて簡単になりすぎるのを避けるため、伸びる軸を絞る方針）

### ダメージ計算式
- コンボ2/3段目のデフォルトダメージは大きくは変えない
- 敵に防御力パラメータを新設し、同一対象への連撃が続くほど防御を削る（後段ほど通りが良くなる。プレイヤー側の倍率ではなく敵側の防御減衰として実装）
- 無被弾で攻撃を継続する「スーパーコンボ状態」を別カウンターとして持ち、閾値ごとに全体ダメージへ倍率をかける（10コンボ→×1.1、30コンボ→×1.2、被弾でリセット）。既存の`OUGI_GAUGE`とは独立

## 2026-08-24（剣戟の森: 武器パラメータ体系 実装第1弾）

上記設計合意に基づき、ロジック層（`src/logic/loadout.ts`）とアウトゲーム画面（`src/scenes/LoadoutScene.ts`）を実装した。GameScene（インゲーム側）への統合は未着手。

- ステータスは「品質(0..1)を各ステータスの実値域にlerpする」方式で表現。レア度Tierごとに品質のロール範囲を割り当て（N:0-0.2 / R:0.2-0.45 / SR:0.45-0.7 / SSR:0.7-0.9 / UR:0.9-1.0）、テンプレートごとの`bias`（ステータスごとの重み、1.0が基準）を品質に掛けてから各ステータスの値域にlerpする。これにより「同品質なら距離重視テンプレートの方が距離は長いが、レア度（品質）が上がれば他のテンプレートも総合力で追いつける」という要件を、距離だけを特別扱いする分岐なしに素直な計算式で実現できた。
- 強化（enhanceWeapon）はパワー/速さのみを乗算で伸ばす設計にした。品質そのもの（=他の4ステータス）はいじらないことで、「強化しすぎると全部伸びて簡単になりすぎる」を避けつつ、ユーザーが指定した「パワーと速さがマスト」を満たす。コンボ数だけは強化レベル2ごとに+1という副次効果を持たせ、「幅を持たせたい」という要望に応えた。
- 進化（evolveWeapon）は強化上限（レベル5）到達後にのみ可能で、進化すると強化レベルは0に戻り品質に加算ボーナスが乗る、という一般的なガチャゲー方式にした。
- 熟練度（proficiency）は命中数の累計を保持するだけのシンプルな実装にし、コンボ数ボーナスとして間接的に反映（500命中ごとに+1、上限+3）。
- インゲーム内重複強化（`RunWeaponState`）は設計合意通り「入手＋強化2回＋アルティメット化1回」の最大4段階（`RunEnhanceStage` 0..3）。アルティメット化（stage3）はレア度がSR以上（`canUltimate`）でなければ`stackRunWeapon`が弾く。これはロードアウト武器個体本体の`enhanceLevel`/`evolutionStage`とは完全に別のフィールドで、ステージをまたいで保持しない想定（GameScene側の実装時にステージ開始でリセットする）。
- アウトゲームのセーブは`potion-workshop`の`save.ts`と同じ`KVStore`インターフェース注入パターンを踏襲（`saveLoadout`/`loadLoadout`）。壊れたJSONや初回未セーブ時は`newLoadoutSave()`にフォールバック。
- `LoadoutScene`は初回プレイ時のみ通貨500を配布し、宝箱（100通貨、確率抽選）と鍛治（テンプレート指定でN固定・50通貨、確定入手）で武器を集められるようにした。スロットへの割当時、テンプレートの`kind`と割当先スロットが一致しない場合は弾き、ヒントメッセージを表示する。Playwrightでのスクリーンショット確認で、宝箱入手・スロット割当・種別不一致の拒否をそれぞれ確認済み。
- **未実装（次のスコープ）**: GameScene（インゲーム側）は今回のロードアウト機構をまだ一切参照していない。現状のインゲームは従来通り`combat.ts`の`WEAPONS`（固定値の近/中/遠3種）で動作する。次に着手する際は、ステージ開始時に`baseEquipmentStats`ベースの弱い武器からスタートし、召喚媒体ピックアップで`effectiveStats(instance)`（＋`runEffectiveStats`のインゲーム内重複強化）に切り替える流れをGameScene側に実装すること。あわせて、ヴァンサバ風の一時停止選択UI・武器を受け取る演出・敵の防御力パラメータ・コンボ継続ダメージ増加・無被弾スーパーコンボ倍率も未着手。

## 2026-08-24（剣戟の森: 武器パラメータ体系 実装第2弾 — GameScene統合）

前回の未実装スコープを実装した。ユーザーからの「今の要求に必要な機能を全部実装してください」という指示を受け、設計合意していた範囲を一通りインゲームに繋いだ。

- `combat.ts`に`PlayerState.customWeapons: Partial<Record<WeaponKind, WeaponDef>>`を追加し、`currentWeapon`が`customWeapons`優先でWEAPONSにフォールバックするようにした。既存の`newPlayer()`はcustomWeapons空で始まるため、既存のテスト・挙動は完全に無傷（デフォルトはWEAPONSのまま）。`setCustomWeapon`/`clearCustomWeapon`で外部（GameScene）から実効武器を差し替えられるようにした、という最小侵襲の設計。これによりロードアウト/召喚システムを、既存の`canAttack`/`startAttack`等の全ロジックを一切書き換えずに統合できた。
- `loadout.ts`に`toWeaponDef(stats, kind)`（BaseWeaponStats→WeaponDef変換）と`resolveSummon(loadout, inventory, kind, existingRun)`（召喚実行の中心ロジック：未設定/未所持ならnull、既に同じ個体を召喚済みなら`stackRunWeapon`で重複強化、そうでなければ`summonRunWeapon`で新規召喚）を追加。GameSceneはこの2関数を呼ぶだけで済むようにし、Phaser側にロジックを持たせないという既存方針を維持した。
- GameSceneのシーンキーを`"game"`→`"GameScene"`に変更（`LoadoutScene`からの`scene.start("GameScene", ...)`と一致させるため。旧キーのままだと遷移が失敗していた）。
- ステージ開始時（`create()`）は3スロットとも`baseEquipmentStats(kind, baseEquipmentLevels[kind])`を`setCustomWeapon`で適用してからプレイを始める。`init(data)`でLoadoutSceneから渡されたロードアウト/所持品/基本装備レベルを受け取り、Rキーでのシーン再スタート等data無しで開始された場合は`loadLoadout(localStorage)`にフォールバックする。
- 召喚媒体・防具・アイテムの3種のピックアップをステージ上に配置（`buildPickups`）。当初ピックアップの物理判定を`GROUND_Y-90`に置いたところ、プレイヤーの当たり判定（`setSize(18,32).setOffset(6,8)`から算出される垂直レンジ）と重ならず拾えない不具合があった。Playwrightでの実操作確認で発覚し、`GROUND_Y-24`に修正して解消（ジャンプ不要で歩くだけで確実に重なる高さ）。
- 召喚媒体を拾うと`this.physics.pause()`で一時停止し、ヴァンサバ風の選択UI（近/中/遠の3枠、1/2/3キーまたはクリックで選択、ロードアウト未設定のスロットは「(未設定)」とグレー表示）を表示する。選択すると`resolveSummon`→`setCustomWeapon`で即座に武器が切り替わり、空中から武器が降ってきてプレイヤーが受け取る演出（Tweenでアイコンが降下→カメラフラッシュ→プレイヤーが一瞬拡大→フローティングテキスト）を再生してからオーバーレイを閉じる。ユーザーが要望した「武器/装備/アイテムはアウトゲーム設定したものから選ぶ」の実装で、ステージ側から提示される一時バフ（ヴァンサバのレベルアップ選択のような別プール）は今回のスコープ外として`docs/TODO.md`に残した。
- 敵に`EnemyState.defense`を追加し、`damageEnemy`で`effectiveDamage = max(1, amount - defense)`を適用した後`defense`を`DEFENSE_SHRED_PER_HIT`（1）ずつ削る方式にした。連続ヒットが続くほど防御が削れて後段の通りが良くなる、という合意通りの挙動。ステージ後半の敵（インデックス4以降）にのみ`defense: 2`を持たせ、防御貫通の手応えの差を演出した。
- 無被弾スーパーコンボは`PlayerState.comboStreak`（命中のたびに+1、`damagePlayer`が実際にダメージ処理を行うたび0にリセット）と`superComboMultiplier(streak)`（10で×1.1、30で×1.2）で実装し、`GameScene.applyHit`で`Math.round(damage * multiplier)`として敵への実ダメージに反映した。
- 防具は`PlayerState.armorCharges`（`gainArmor`で加算）とし、`damagePlayer`をHPより先に耐久を1消費するよう変更。耐久で防いだ場合も無敵時間の付与とコンボリセットは行う（「被弾」自体は発生している）。アイテムは所持一覧UIまでは作らず、拾った瞬間に`useItem`→`healPlayer`でHP+1する簡略実装とした（複数種のアイテム/所持UIは`docs/TODO.md`に未着手として記載）。
- Vitest 22件追加（combat.ts 14件、loadout.ts 8件）。計103件。`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`すべて✅。Playwrightで、宝箱→スロット割当→ステージ開始→召喚媒体ピックアップ→選択UI表示→武器召喚（「近接 召喚！」のフローティングテキスト確認）までの一連の流れを実操作で確認済み。

## 2026-08-24（剣戟の森: 武器パラメータ体系 実装第3弾 — アイテム/防具/ステージバフ）

ユーザーから「全部止まらずに進めて」との指示を受け、前回の実装で未着手として残していた3項目（アイテム所持一覧UI・複数種のアイテム、防具のアウトゲーム設定、ステージ側から提示される一時バフ）をすべて実装した。

- **一時バフの共通基盤**: アイテムの護符とステージバフの両方が「攻撃力アップ」「俊足」を提供する必要があったため、`combat.ts`に`PlayerState.buffs: { power?: number; haste?: number }`（値は効果が切れる時刻）という汎用の仕組みを新設し、`applyBuff`/`isBuffActive`/`buffDamageMultiplier`/`buffSpeedMultiplier`を実装。アイテム側は`ITEM_BUFF_DURATION_MS`（8秒）、ステージバフ側は`STAGE_BUFF_DURATION_MS`（12秒）と持続時間だけ変えて同じ関数を再利用しており、「アイテムはアウトゲーム設定、ステージバフは独立プール」という別々の入手経路を持ちながら効果の実装自体は共通化した。
- **HP自動回復**（ステージバフの1種）は`applyRegen`/`tickRegen`として別途実装。`REGEN_TICK_MS`（1秒）ごとに`REGEN_TICK_AMOUNT`（1）だけ回復し、`GameScene.update`で毎フレーム`tickRegen`を呼ぶ方式にした。
- **アイテム**: `loadout.ts`に`ITEM_DEFS`（ポーション/剛力の護符/俊足の護符）を定義。ステージ上のアイテムピックアップは即時消費ではなく所持数を増やすだけにし（`Pickup.itemId`を持たせ、拾った位置に応じて種類を巡回させる）、使用は専用キー（Z/V/B、1/2/3は武器切替に使っているため別キーにした）で行う。HUDに常時「Z:ポーション×N V:剛力の護符×N B:俊足の護符×N」を表示し、所持数0でキーを押すと「所持していない」というヒントを出すガードも入れた。
- **防具のアウトゲーム設定**: ユーザーの要望は「武器/装備/アイテムはアウトゲーム設定したものから選ぶ」だったため、防具にもアウトゲームでの選択肢を用意した。ただし武器のようなレア度ロール・個体差・所持在庫までは今回のスコープでは過剰と判断し、`ARMOR_TEMPLATES`（なし/革の鎧/鎖帷子/板金鎧、Tierが上がるほど耐久・コストが増加）から1つを選ぶだけのシンプルな方式にした。選択のたびにコストを支払う「出撃ごとに選び直す消耗前提」の設計で、`LoadoutSaveData.selectedArmorId`として永続化し、ステージ開始時に選択Tierの耐久を`gainArmor`で付与する。
- Vitest 16件追加（combat.ts 10件、loadout.ts 6件）。計119件。`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`すべて✅。
- Playwright検証中、召喚媒体オーバーレイが期待通りに開閉しない（ように見える）事象があったが、一時的に`console.log`でトレースした結果、実際にはテストスクリプトの待機時間が短すぎてプレイヤーがまだピックアップ地点まで歩き切っていなかっただけで、アプリ側のバグではなかったことを確認（デバッグログは検証後に削除済み）。この経緯から、Playwrightでのタイミング検証では「移動距離÷MOVE_SPEED」から逆算した十分な待機時間を確保すること、という教訓を得た。
- 実操作確認: 防具パネルでの購入・選択（革の鎧を選択し通貨が正しく減ることを確認）、ステージ開始後に敵を撃破してスコア/必殺ゲージが正しく増えること、アイテム未所持時にキーを押すとガードメッセージが出ることを確認済み。

## 2026-08-25（剣戟の森: モバイル対応の設計・実装）

ユーザーから「スマホ対応できる？」と聞かれ、まず設計だけを提示（実装はしない）。その後「抜け漏れなく明確かつMECEな設計を心がけてください」との指摘を受け、既存コードを監査して設計を精緻化してから実装した。

### 設計方針（MECE）
- 対象画面は`LoadoutScene`と`GameScene`の2つのみ（漏れなく全て）
- `GameScene`は`update()`の早期returnの分岐（tipsVisible / summonOverlayVisible / stageBuffOverlayVisible / status!=="playing" / 通常プレイ）をそのまま状態表として扱い、各状態で有効な入力アクションを列挙。実装（コードの分岐構造）と設計（状態表）を一致させることで漏れを防いだ
- 監査の結果、既存コードに2つの抜けを発見: TIPSオーバーレイの背景がinteractiveなのにpointerdownハンドラが無く、タップでは閉じられなかった。ゲームオーバー/クリア画面のリトライテキストがinteractiveですらなく、タップでリトライできなかった。どちらも「新規実装」ではなく「既存コードの修正」として設計に含めた

### 実装の中核設計判断
- Phaserの`Key`クラスのソースを直接確認し、`Key#onDown(event)`/`Key#onUp(event)`が実際のキーボードイベント受信時に呼ばれるのと全く同じ処理（`isDown`/`_justDown`のセット）を行う公開メソッドであることを確認した。これにより、仮想ボタンのpointerdown/pointerupから既存の`Key`オブジェクトへ直接`onDown`/`onUp`を橋渡しするだけで、`handleMovement`/`handleAttack`/`handleSkill`/`handleWeaponSwitch`/`handleItemUse`など既存の判定ロジックを一切変更せずにタッチ入力を統合できた（`src/ui/touch.ts`の`bindHeldKey`）
- **発見して修正した実バグ**: `Key#onUp`は`isDown`/`_justDown`を無条件でリセットする。タップ操作は`touchstart`→`touchend`が同一フレーム内で処理されることがあり（Phaserの入力処理は`Scene#update`より前の`PRE_STEP`で走るため）、その場合`Scene#update`が`JustDown`を読む前に`onUp`が`_justDown`を`false`に戻してしまい、瞬間タップの入力（武器切替・アイテム使用など）が消えてしまう事象を実機シミュレーション（Playwrightのタッチエミュレーション）で発見した。`scene.time.delayedCall(0, ...)`で遅延させる案を試したが、Phaserの`Clock`更新も同じフレームの`Scene#update`より前に走るため対策にならないことをソースコード（`Clock.js`/`TimerEvent.js`）で確認。最終的にブラウザ標準の`requestAnimationFrame`（次の描画フレーム＝今フレームの`Scene#update`完了後まで確実に遅延）で`onUp`を遅延させることで解消した。修正前後をPlaywrightで比較し、修正前は`装備: 近接`のまま変化しなかった武器切替タップが、修正後は`装備: 中距離`→`装備: 遠距離`と正しく切り替わることを確認した
- タップ判定サイズは「単独ボタン（宝箱ボタン、スロット、開始ボタン等）は44px以上」「密なリスト行（所持武器一覧、鍛治/防具の選択肢、行間20px）は44px強制だと隣の行と重なってしまうため強制せず、高さは行間ぎりぎりまで・横幅は画面端まで広げる」という2方針に分け、`makeTappable`はサイズを強制しない汎用ヘルパーとし、呼び出し側で明示的に指定する設計にした
- 縦向き対策は`buildOrientationWarning`を`GameScene`専用で実装した後、`LoadoutScene`側に無いことに気付き（両画面が対象という設計方針との齟齬）、`src/ui/touch.ts`の共通関数として抽出し両シーンから呼ぶよう修正した
- `index.html`に`maximum-scale=1.0, user-scalable=no`（ダブルタップズームの誤爆防止）と`touch-action: none; overscroll-behavior: none`（プルツーリフレッシュ等のブラウザジェスチャー抑止）を追加
- 既存コード変更なしで済んだ箇所: 召喚媒体/ステージバフの選択オーバーレイ、LoadoutSceneのスロットzone等は元々`setInteractive({ useHandCursor: true })`のRectangle/Zoneで実装されており、Phaserのポインター機構がマウス/タッチを統一的に扱うためそのままタッチ対応していた
- 残課題: 実機（iOS Safari/Android Chrome）での確認は未実施（Playwrightのタッチエミュレーションのみ）。ゲームオーバー/クリア画面表示中も仮想ボタンが視覚的に残る点（機能的には無害、`update()`の早期returnで無視される）は見た目の整理余地として`docs/TODO.md`に記載

## 2026-08-25（剣戟の森: 動的仮想スティックへの置き換え）

ユーザーから「デジタルパッドもっと今風にできる？表示しないとか」との要望を受け、固定表示だった4方向D-padボタンを動的仮想スティックに置き換えた。

- `src/ui/touch.ts`に`bindVirtualJoystick`を追加。左半分（x:0-400, y:0-600）を丸ごと入力ゾーンとし、`pointerdown`があった座標を中心にベース円＋つまみ円を表示、`pointermove`で指の動きにつまみが追従（`maxRadius`でクランプ）、`pointerup`で両方非表示に戻す。方向判定は原点からの相対距離がデッドゾーン（14px）を超えたかどうかで`left/right/up/down`それぞれ独立に真偽判定し、`bindHeldKey`と同じ`Key#onDown`/`onUp`橋渡しパターンで既存の`cursors`判定ロジックを無変更のまま流用した。JustDown消失防止のrequestAnimationFrame遅延も同様に適用。
- 常時表示のボタンをやめたことで「表示しない」という要望通り、触れるまで画面に何も出ない状態になった。攻撃/スキル/武器切替/アイテム使用/TIPSの右側ボタン群は変更していない（要望は明示的にD-pad＝移動操作についてのものだったため）。
- Playwrightでの検証はタッチ操作特有の難しさがあった。`new Touch()`/`TouchEvent`を手動構築してcanvasにdispatchする方法ではPhaserのInputManagerに拾われず（Phaser 4はPointer Events APIベースで、生のTouchEventを直接listenしていないと見られる）、`page.mouse.move/down/move/up`のシーケンスに切り替えて検証した（Playwrightの`isMobile`/`hasTouch`コンテキストではマウスイベントも同じポインタープラグインで処理されるため機能した）。この切り替えでスティックの出現・追従・召喚媒体ピックアップまでの移動・release後の非表示までを確認できた。

## 2026-08-25（両ゲーム: UIビジュアル刷新）

ユーザーから「UIをもっと行けてる感じにして」との要望を受け、対象を確認したところ「両方（ポーション工房・剣戟の森）」との回答。両ゲームともフラットな`add.rectangle`＋単色塗りが中心だったのを、角丸パネル・グラデーション背景・グロー枠を使ったモダンな見た目に刷新した。

### 剣戟の森（`games/side-scroller/src/ui/theme.ts`、新規）
- `drawPanel(scene,x,y,w,h,options)`: Graphicsで角丸パネルを描画。下寄りの柔らかい影＋上部のガラス風ハイライト帯を重ねる。HUDパネル、TIPS/召喚媒体/ステージバフの各オーバーレイ、LoadoutSceneの全パネルに適用。
- `makeButton(scene,x,y,w,h,label,onClick,options)`: 角丸＋ホバー/押下フィードバック（`pointerover`/`pointerout`/`pointerdown`で塗り色を切り替え、押下時に軽くスケールダウンするtween）付きのボタン。LoadoutSceneの宝箱ボタン・ステージ開始ボタンに使用。
- 召喚媒体/ステージバフの選択カードは、視覚は`drawPanel`（非interactive）、当たり判定は透明な`Rectangle`（interactive、hoverで枠線を`Graphics.lineStyle`で描き直す）という2層構成にした。1つのGraphicsに複雑なホバー状態を持たせるより、視覚と入力判定を分離した方がシンプルだったため。
- LoadoutSceneはレイアウト衝突が複数発生した: (1) 所持武器一覧パネルを画面幅いっぱいに描くと右側の宝箱/防具パネルと二重に箱が重なって見える問題→左カラムのみに幅を絞った。(2) ステージ開始ボタンをmakeButton化して少し大きくした結果、防具パネルの右下と当たり判定が数十px重なる問題→ボタンを防具パネルの下（y=545）に配置し直して解消。既存のcoordinateベースレイアウトにGraphicsパネルを足す作業は、要素ごとの矩形範囲を都度計算して重なりを確認する必要があり、Phaserにはレイアウトエンジンが無いため手動調整が中心になった。

### ポーション工房（`games/potion-workshop/src/ui/theme.ts`、新規）
- こちらは`refreshUI()`が毎フレーム`.setFillStyle(color)`を呼んで購入可否を色で示す設計だったため、単純に`add.rectangle`をGraphicsへ置き換えると全呼び出し箇所の書き換えが必要になり影響範囲が大きくなる。そこで`makeRoundedRect(scene,x,y,w,h,fillColor,options)`が、返り値に`setFillStyle`互換メソッドをモンキーパッチした`Graphics`（`RoundedRect`型）を返す設計にし、既存の`this.clickUpgradeButton.setFillStyle(...)`等の呼び出しをほぼ無変更のまま角丸化できるようにした。
- Graphicsは中心を原点としたローカル座標で描画し、GameObject自体を`(x,y)`に配置する方式にした。理由は、生成ボタン（`buildGeneratorList`）が`tweens.add({targets:[button,label], scaleX:1.03,...})`で購入時に拡大演出を行っており、Graphicsを絶対座標で描くとスケールが左上基準になってボタン中心からズレて見える問題があったため。ローカル座標＋GameObject位置指定にすることで、既存のtweenコードを一切変更せずにスケール演出が中心基準のまま動くようにした。
- ブリューボタン（クリックの起点）は単色円のままだと寂しいため、外側にうっすらとしたグロー円と、内側にストロークだけのリングを追加して二重リング風の演出にした。クリック時のtweenも`ring`を含めて縮小させ、リングごと脈動するような手応えを足した。
- 背景は単色黒からグラデーション（濃紺→紫がかった紺）と放射状のうっすらとしたグローに変更。既存の浮遊する泡アニメーションはそのまま活かした。
- Vitest（66件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行、Playwrightで実際の画面を確認（LoadoutScene一式、GameScene HUD/TIPS/召喚媒体オーバーレイ、ポーション工房のブリュー操作・実績モーダル）。

## 2026-08-25（続き）: UI/UX洗練（タイポグラフィ/マイクロインタラクション）
- ユーザーから「UIUXもっと洗練させたいね」との要望を受け、AskUserQuestionで方向性を確認したところ「タイポグラフィ/余白」「マイクロインタラクション」「情報設計/UXフロー」の全部が選択された（今回はタイポグラフィとマイクロインタラクションに着手、情報設計/UXフローは次回以降）。
- `theme.ts`に`TYPE`タイポグラフィスケール（h1/h2/numeric/body/small、共通フォントスタック＋見出しの字間調整）を両プロジェクトに追加。画面ごとにバラバラだった`fontSize`/`fontStyle`のベタ書きを役割ベースのトークンに置き換える方針にした。
- ホバー演出をボタンヘルパー内部に完結させる設計にした。理由: ポーション工房の`refreshUI()`は毎フレーム`.setFillStyle(color)`を呼んで購入可否を色で示すため、呼び出し側で単純にホバー色を上書きすると次フレームで即座に上書きされてちらつく。そこで`makeRoundedRect`内部に`baseColor`/`hovering`状態を持たせ、`paint()`クロージャで常に「ホバー中ならbaseColorを白方向にブレンド、そうでなければbaseColorそのまま」を描画する方式にし、`setFillStyle`は`baseColor`の更新のみ行うようにした（`hoverLighten`オプションで無効化も可能）。
- `SmoothedCounter`（指数平滑化: `displayed += (target-displayed)*min(1,speed*deltaSec)`、閾値以下でスナップ）をポーション工房のポーション数表示に導入。放置ゲームの数値が毎フレーム変わる箇所を、瞬間ジャンプでなく滑らかに動く見た目にした。オフライン進行の大きな増分でいきなりアニメーションしないよう、セーブロード直後に現在値で`SmoothedCounter`を再初期化する。
- `popOnChange`（テキストが変化した時だけ軽くスケールポップさせる）を、ポーション工房のエッセンス表示と剣戟の森の武器切替表示（頻度の低い離散的な変化）に適用。毎フレーム変わる値ではなく、たまに変わる値に気付きやすくする用途に限定した。
- Vitest（potion-workshop 66件、side-scroller 119件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行済み、全て成功。

## 2026-08-25（続き2）: 階層デザインシステムとアイコンの環境依存バグ修正
- PR #19マージ後、スクリーンショットを見せたところユーザーから「まだ全然いけてない、ベテランデザイナー目線でチューニングして」と率直な評価を受けた。診断として（1）背景/パネル/カードの明度差が乏しく階層が一枚岩に見える、（2）余白のリズムがなくグルーピングが読みにくい、（3）アクセントカラーの意味が不統一、（4）絵文字がヘッドレスChromium環境で豆腐化する実害バグ、の4点を提示し合意を得た。
- さらに「最終的にはコンシューマ/ソシャゲのようにしたい」という要望に対しては、絵・アイコン素材やBGM等の「素材」はゼロから制作できない一方、配色・階層・余白・演出などの「デザインシステムと情報設計」はコードで作り込める、という現実的な期待値合わせを行い、まず階層/余白/配色の底上げから着手する方針で合意した。
- `theme.ts`（両プロジェクト共通の考え方）に`ELEVATION`（bg/zone/cardの3段階の明度トークン）と`SPACE`（4/8/16/24/32pxの余白スケール）を追加。
- ポーション工房: ブリューエリアと設備リストをそれぞれ1枚の`drawPanel`ゾーンパネル（`ELEVATION.zone`、fillAlpha 0.9）で囲み、背景と同化していたセクションの境界を明確化。タイトルの🧪絵文字はスクリーンショットで空の四角（豆腐）として描画されることを実機確認したため、`drawFlaskIcon`でGraphics描画のオリジナルフラスコアイコンに置き換えた（環境のフォント/絵文字セットに依存しなくなる）。
- 剣戟の森: LoadoutSceneの武器一覧/宝箱・鍛治/防具の3ゾーンパネルを`fillAlpha 0.55`（背景に溶け込みやすい半透明）から`ELEVATION.zone`色×`fillAlpha 0.92`（はっきり手前に見える不透明に近い塗り）に変更し、パネルの視認性を改善。既存の`RARITY_COLOR`によるレアリティ色分けボーダーはソシャゲ的な演出として既に妥当だったため維持。
- Vitest（potion-workshop 66件、side-scroller 119件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行済み、全て成功。Playwrightでスクリーンショットを取得し、豆腐化解消とパネルコントラスト改善を目視確認済み。

## 2026-08-25（続き3）: デザインレビューでの枠の重なり修正
- 直近のスクリーンショットに対しユーザーから「枠の重なりなど、デザイン目線で見て欲しい」と指摘を受け、座標計算とPillow(PIL)でのクロップ拡大により精査した。
- 発見した実害バグ: (1) ポーション工房のサウンドボタン🔊/🔇絵文字が、タイトルの🧪と同様にこの環境で判読不能な塊に潰れて表示される。`drawSpeakerIcon`（`theme.ts`）でGraphics描画のオリジナルスピーカーアイコンに置き換えて解消。
- 発見した余白/重なりの問題: (2) ポーション工房の転生ボタン下端とゾーンパネル下端の余白が8pxしかなくほぼ密着していた → パネル高さを450に拡張。(3) 剣戟の森のLoadoutSceneで、近/中/遠距離スロットパネルの下辺(y=175)と武器一覧ゾーンパネルの上辺(y=175)が完全に隙間ゼロで接触し、異なる色の枠線同士が衝突して見えていた → パネルを10px下げて隙間を確保。(4)「宝箱を開ける」ボタンが自身の属するゾーンパネルの上端(y=180)から8pxはみ出して描画されていた（ボタンtop=172 < パネルtop=180）→ パネルとボタンの位置・サイズを再計算し完全に内包させた。
- (3)(4)はいずれも数値ベースの目視だけでは気づきにくい「ピクセル単位の座標計算ミス」だったため、スクリーンショットをクロップ拡大して確認する手順を踏んだ。今後もパネル/ボタンを新設する際は、隣接要素との境界座標を明示的に計算して余白を確保する。
- Vitest（potion-workshop 66件、side-scroller 119件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行済み、全て成功。修正後のスクリーンショットで重なり解消を目視確認済み。

## 2026-08-25（続き4）: データ可視化・フッター活用
- ユーザーから「もっとビジュアライズして、フッターなども駆使して欲しい」との要望を受け、AskUserQuestionで方向性を確認（複数選択）「データの可視化」「余白の活用」「装飾・演出の強化」の3つが選ばれた。今回は前2つに着手。
- `theme.ts`（ポーション工房）に`drawProgressBar`を新設。トラック(下地)とフィル(進捗)を別々のGraphicsに分離し、`setRatio(ratio)`で軽量に更新できる設計（`RoundedRect`の`setFillStyle`パターンと同じ思想）。
- ポーション工房: 転生カードに「累計醸造数 / 転生解放ライン(PRESTIGE_UNLOCK)」の進捗バーを追加。既に転生解放済みの場合はバーを非表示にする（`gained > 0`で判定）。
- ポーション工房: 画面下部の空白（ゾーンパネル下端y≈585〜602〜キャンバス下端760）を`drawPanel`によるフッターパネルとして活用し、これまで数値化されていなかった生涯統計（`lifetimeBrewed`・実績解除数/10・`prestigeCount`・`analytics.totalPlaytimeSec`）を1行で表示。既存のExport/Importボタンはフッター内に統合し、見た目上も機能的にも「設定・データ管理エリア」としてまとめた。
- 剣戟の森: LoadoutSceneの武器一覧見出し直下に、所持武器のレアリティ内訳（N/R/SR/SSR/UR）を`RARITY_COLOR`で色分けした積み上げ横棒グラフとして表示（`renderRarityBar`）。武器を新規入手するたびに`refresh()`から再計算する。データが空の場合は暗いトラックのみ表示。
- レイアウト調整: 進捗バー追加に伴い転生カード内のテキスト位置を上に詰め、フッターパネル追加に伴いExport/Importボタンをy=730→715へ移動。剣戟の森はレアリティバー追加により武器一覧の見出し・行開始位置を14px分下にずらし、隣接要素との重なりが再発しないよう座標を明示的に計算した。
- Vitest（potion-workshop 66件、side-scroller 119件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行済み、全て成功。Playwrightで宝箱を複数回開いた状態のスクリーンショットを取得し、レアリティバーが実データを反映して描画されることを目視確認済み。

## 2026-08-25（続き5）: 装飾・演出の強化
- 「データの可視化」「余白の活用」に続き、AskUserQuestionで選ばれていた3つ目の方向「装飾・演出の強化」に着手。
- ポーション工房: 転生ボタンが解放された瞬間だけパルスするグローを追加。`refreshUI()`は毎フレーム呼ばれるため、単純にtweenを毎回addすると大量のtweenが積み重なってしまう。そこで`prestigeWasAffordable`フラグで「未解放→解放」の遷移を検知した瞬間だけ`tweens.add`し、「解放→未解放（転生実行後）」の逆遷移でtweenを停止する設計にした。グロー用のGraphicsはボタンより先に生成し描画順で必ず背後に来るようにした。
- ポーション工房: 設備一覧の各行に、`Phaser.Display.Color.HSVToRGB`で設備インデックスごとに色相をずらしたジェム風アイコン（小円+ハイライト+ストローク）を追加。8種類の設備が一目で見分けやすくなるよう、ラベルテキストのx座標もアイコン分だけ右にずらした。
- 剣戟の森: 近/中/遠距離スロットが未設定の間、パネル外周を無限ループでゆっくりパルスさせるグローのGraphicsを重ねて表示し、空きスロットの存在に気付きやすくした。武器を装備すると`refresh()`内でグローを非表示にする（tween自体は止めず、`setVisible(false)`のみで制御し、再度未設定に戻った時にすぐ再表示できるようにした）。
- 剣戟の森: 武器一覧のレアリティ表示にソシャゲ的な★演出を追加（SR=★/SSR=★★/UR=★★★、N/Rは無印のまま）。既存の`RARITY_COLOR`によるテキスト色分けと組み合わせることで、レアリティの視認性を二重に高めた。
- Vitest（potion-workshop 66件、side-scroller 119件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行済み、全て成功。

## 2026-08-25（続き5）: 世界観の刷新（淡い青空・ファンタジー系）
- ユーザーから「色味などの世界観もっと明るく、ソシャゲ調のビジュアルにしたいな。もっといい感じにしたい」との要望を受けた。トーンが曖昧だったためAskUserQuestionで「パステル×ポップ」「ビビッド×ハイコントラスト」「淡い青空・ファンタジー系」の3択を提示し、「淡い青空・ファンタジー系」が選ばれた。
- 両プロジェクトの`theme.ts`にある`THEME`/`ELEVATION`を、濃紺ベース（`panelFill: 0x181830`、`ELEVATION.bg: 0x0f1022`等）から水色〜白グラデーション基調のパステル配色（`panelFill: 0xf3f9ff`、`ELEVATION.bg: 0xcfe9ff`等）に刷新した。これがパネル・階層カラーの唯一の情報源になっているため、この2定数の変更だけで両ゲームの「土台」を一括で明るくできた。
- パネルが明るくなったことで、これまでの「明るい文字色 on 暗い背景」という配色設計が反転して読めなくなる（例: `#e0e0ff`のほぼ白いテキストが白いパネルの上で消える）ため、`textPrimary`/`textMuted`を濃紺〜紺青系の暗色に変更し、加えて各シーンにベタ書きされていた個別の文字色（`#ccccdd`、`#aaaacc`、`#e8e8fb`、`#d9a7ff`等の「暗背景用の淡色」）も、同系統だが十分暗い色（`#3a4a5a`、`#7488a0`、`#8a4fd1`等）へ手動で置き換えた。単純な反転ではなく、色相を保ったまま明度だけ下げる方針にし、既存の「装備種別=ピンク/金/水色」のようなセマンティックな色分けは維持した。
- `makeRoundedRect`のホバー演出は元々「基準色を白方向にブレンド」する実装だったが、明るい配色では白へのブレンドがほぼ効かなくなるため、水色（`0x8ecbf5`）方向へのブレンドに変更した。`drawPanel`のガラス風ハイライト帯（白, alpha 0.05）も暗背景でしか見えない設計だったため、alphaを0.4前後に引き上げて明るいパネル上でも「艶」が見えるようにした。
- 剣戟の森はGameSceneが実際のプレイフィールド（空・地面・敵）を持つため、UIテーマ（テーマトークン）だけでなくゲーム世界そのものの配色も見直した: `main.ts`のcanvas背景色、`buildLevel()`の空（フラット矩形→Graphicsグラデーション）、地面（暗い紫がかった色→茶色い土+緑の芝）、浮遊足場（暗いスレート色→白い「雲」に変更、ソシャゲ/ファンタジー世界観との親和性を狙った）。敵・プレイヤーのスプライト色（`drawHumanoidTexture`）は識別性に関わるゲームプレイ要素のため意図的に変更対象から外した。
- `spawnFloatingText`ヘルパーに白フチ（`stroke: "#ffffff", strokeThickness: 3`）を追加。多数の呼び出し箇所ごとに色を全部作り直すより、ヘルパー側で「明るい背景でも読める」を保証する方が保守性が高いと判断した。
- Vitest（potion-workshop 66件、side-scroller 119件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行済み、全て成功。Playwrightでポーション工房のメイン画面、剣戟の森のロードアウト画面・宝箱開封後の武器一覧・ゲーム画面を確認し、パステル調の空、白パネル、可読なテキストコントラストを目視確認済み。

## 2026-08-25（続き6）: UIのスッキリ化・ゲームらしい見た目への刷新
- ユーザーから「UIをもっとスッキリ、あとから差し替える前提でもっとゲームらしい見た目にしたい」との要望を受けた。AskUserQuestion（複数選択）で「剣戟の森: 武器一覧をカード型グリッドに」「ポーション工房: 設備リストの情報量を削減」の両方が選ばれた。
- ポーション工房: 設備リストの各行が「Apprentice Alchemist ×0\nCost: 15   +0.5/sec」のように1行にテキストを詰め込んでおり、スプレッドシート的で見づらかった。名前+所持数を主役の1行にし、生産量（+X/sec）は小さく補足として下段に、コストは行の右端に「値札」として分離配置（`costLabel`を新設、`row.label`/`row.rateLabel`/`row.costLabel`の3テキスト構成に変更）。ショップの商品行のような視覚的な構造にし、購入可否の色分けは値札（コスト）側にのみ適用することで「買えるかどうか」の情報がより直接的に伝わるようにした。
- 剣戟の森: LoadoutSceneの武器一覧が「鉄の剣 [SR] ★ 距離177 力4.4 速257ms 範囲51 重3.8 コンボ4」のような長いステータス羅列のテキスト行（1行20px間隔でひたすら縦に並ぶ）で、まさに「ゲームらしくない」スプレッドシート的UIの典型例だった。3列×3行のカード型グリッド（`INVENTORY_GRID`定数で列数/カードサイズ/開始位置/最大表示行数を一元管理）に刷新し、各カードは「アイコン＋名前＋レアリティ（色枠+★演出）」のみのミニマル表示にした。詳細ステータス（距離/力/速度/範囲/重さ/コンボ）はカードから削り、選択中の1件だけをグリッド下の`inventoryDetailText`にまとめて表示する設計にすることで、情報を捨てずに「一覧性」と「詳細さ」を分離した。
- カードのアイコンには`drawWeaponKindIcon`（`theme.ts`に新設）を使い、近距離=剣、中距離=槍、遠距離=弓のシルエットをGraphicsで描画した。本物の武器アイコン素材への差し替えを前提としたプレースホルダーであることをコメントに明記し、テキストの武器種名だけよりも一目でゲームらしく見えるようにする狙い。
- 9件を超える所持武器がある場合にカードから溢れた分を黙って切り捨てず、「他N件」という補足テキストを表示するようにした（`INVENTORY_GRID.maxRows`のキャップに対する透明性を確保）。
- Vitest（potion-workshop 66件、side-scroller 119件）は見た目の変更のみで対象外。両プロジェクトで`npm run typecheck`/`npm run lint`/`npm test`/`npm run build`実行済み、全て成功。Playwrightで宝箱を複数回開いてカードグリッドに武器を並べた状態、カード選択時の詳細ステータス表示、設備リストの新レイアウトをスクリーンショットで目視確認済み。

## 2026-08-26: 剣戟の森 — 攻撃モーション改善・しゃがみ/ガード/追加ジャンプの追加
- ユーザーから「攻撃モーションはもう少しわかりやすく見やすく」「バフやスキルによって追加ジャンプができると面白そう」「しゃがみを組み込みたい」「ガードも欲しいかも」と4件の要望を一度に受けた。GameScene.tsとcombat.ts/loadout.tsを読み込んで既存の入力・状態管理パターンを把握した上で、それぞれ実装した。
- **攻撃モーション**: 従来は`swordSlash`という1つの`Rectangle`を毎フレーム`setFillStyle(0xffffff, attacking ? 0.4 : 0)`で表示/非表示するだけの実装で、薄い矩形がチラつく程度にしか見えなかった。`spawnAttackFx(weapon)`を新設し、攻撃発動の瞬間に武器種の色（近距離=ピンク/中距離=金/遠距離=水色）を帯びた弧を`Graphics.arc`で描画し、`scaleX`を0.5→1.15へ、`alpha`を0.95→0へtweenさせて「勢いよく広がってフェードする斬撃」に見せる一回限りのエフェクトに置き換えた。合わせてプレイヤー自身に一瞬の白tintフラッシュを追加し「攻撃の手応え」を強調した。当たり判定（`inAttackRange`による距離判定、`weapon.range`）は一切変更していない——見た目と判定を分離したことで、判定ロジックのリグレッションリスクをゼロに保ったまま視覚だけを刷新できた。
- **しゃがみ**: 地上で↓キーを押しっぱなしの間だけ`crouching`状態になる設計にした。既存の`cursors.down`は同時にコマンド入力バッファ（↓トークン、奥義/秘奥義コマンドの一部）にも使われているため、`JustDown`（コマンド用）と`isDown`（しゃがみの継続判定）を素直に両方読むだけで衝突なく共存できた。しゃがみ中は`body.setSize(18,20).setOffset(6,20)`で当たり判定を低くし（オフセットを12px増やすことで足元の位置が変わらないようにした）、`player.setScale(1,0.68)`で見た目も低くする。状態が変化した時だけ`setSize`/`setScale`を呼ぶよう`wasCrouching`フラグでガードし、毎フレームの無駄な再計算を避けた。移動速度は0.35倍に低下させ、機能的な意味（footprintが小さくなる代わりに機動力が落ちる）を持たせた。
- **ガード**: 新規キー（Shift）を割り当て、構え中は盾アイコン（`Graphics`の弧+扇形、プレイヤーの向きに応じて`setScale(facing===-1?-1:1,1)`で反転）を表示する。`onPlayerTouchEnemy`に、ガード中かつ`isFacingTarget`（プレイヤーが敵の方向を向いているか、`facing`を使った内積的な符号判定）がtrueの場合に`damagePlayer`の呼び出し自体をスキップして敵をノックバックさせるだけの分岐を追加した。ガードは「移動と攻撃ができない」というトレードオフにすることで、無敵コマンドにならないようバランスを取った。無敵時間（`invulnerableUntil`）は付与しない設計にしたため、敵の接触判定は毎フレーム発生しうるが、エフェクト自体は`lastGuardBlockAt`によるデバウンス（150ms）で多重発火だけ防いでいる。
- **追加ジャンプ**: 既存の一時バフ機構（`BuffKind`/`isBuffActive`/`applyBuff`、アイテムとステージバフの両方が使う共通の仕組み）に`doubleJump`を追加し、ステージバフの選択肢（`STAGE_BUFF_POOL`）に「空中二段ジャンプ」を新設した。バフとして実装したのは、既存の「アイテム/ステージバフ」という一時強化の枠組みに素直に乗せられ、かつユーザーの要望文言（「バフやスキルによって」）とも合致するため。GameScene側は`airJumpsUsed`カウンタを持ち、地上着地時（`body.blocked.down`）にリセット、空中で`JustDown(cursors.up)`かつバフ有効かつ未使用なら1回だけ追加ジャンプできるようにした。既存の地上ジャンプは`isDown && blocked.down`という「held」判定のままなので、既存のジャンプの手触り（ボタン押しっぱなしで連続ジャンプできる挙動）は一切変更していない。
- モバイル（タッチ操作）では今回ガード用の仮想ボタンを追加していない。既存の仮想ボタン（攻撃/スキル/武器切替/アイテム）配置が800x600の画面内でかなり密集しており、新規ボタンを安全に配置するにはレイアウト全体の見直しが必要と判断したため、今回はキーボード（Shift）のみの対応とし、TODO.mdに残課題として明記した。しゃがみは既存の仮想スティックの下方向でそのまま機能するため追加対応不要。
- Vitest（side-scroller 119件、既存テストのみ。今回追加した機能はGameScene.ts側のPhaser依存コードが中心で、combat.ts側の変更は`BuffKind`/`buffs`型へのユニオン追加のみのため新規テストは不要と判断）は全て成功。`npm run typecheck`/`npm run lint`/`npm run build`実行済み、全て成功。Playwrightで実際にX（攻撃）/↓（しゃがみ）/Shift（ガード）/↑（ジャンプ）を入力し、それぞれの視覚効果が発生することをスクリーンショットで確認済み。

## 2026-08-26（続き）: 剣戟の森 — ステージ制からウェーブ式サバイバルへ変更
- ユーザーから「どちらのゲームもステージをたくさんつくるというより、負荷をかけたりしながらどこまで到達できるかをチャレンジするゲームにする方が面白そうなのでそうしよう」との提案を受けた。両ゲームへの影響を切り分けるためAskUserQuestionを2回行い、（1）剣戟の森は「ウェーブ式サバイバル」（ウェーブごとに難易度上昇、何ウェーブまで生き残れるかを競う）、（2）ポーション工房は既に累計生産で「どこまでいくか」を測れる性質があるため今回は変更なし、という結論になった。
- 固定ゴール（`GOAL_X=3200`の横に長いステージ、`reachedGoal`で"cleared"判定）の設計を、固定サイズアリーナ（`ARENA_WIDTH=1600`）内で完結するウェーブサバイバルに変更した。`combat.ts`の`reachedGoal`/`gameStatus`/`"cleared"`ステータス自体は既存テストが依存しているため削除せず残し、GameScene側で`gameStatus(player, playerX, Number.POSITIVE_INFINITY)`のように到達不可能なgoalXを渡すことで「cleared」分岐を実質的に無効化する形にした。テスト済みの共有ロジックを壊さずに呼び出し側だけ挙動を変える、最小の変更で済む方法を選んだ。
- 難易度カーブは新規モジュール`src/logic/waves.ts`（Phaser非依存の純粋関数）に集約した: `enemiesInWave(wave)`（3体から始まり2ウェーブごとに+1体、上限10体）、`enemySpecForWave(wave)`（体力は3ウェーブごとに+1、防御力はウェーブ4以降で3ウェーブごとに+1、上限5）、`pickupsForWave(wave)`（3の倍数ウェーブは2個、それ以外は1個）。Vitest 10件を追加して境界値・上限を検証した。
- 敵の固定配置リスト（`buildEnemies`）とピックアップの固定配置リスト（`buildPickups`）を廃止し、`spawnWave(wave)`がアリーナ内のランダムなx座標に`enemiesInWave(wave)`体を`enemySpecForWave(wave)`のステータスでスポーンし、`spawnWavePickups(wave)`が同時にピックアップを配置する方式に置き換えた。`onEnemyKilled`でウェーブ内の残り敵数（`waveEnemiesAlive`）を追跡し、0になったら`WAVE_INTERMISSION_MS`（2200ms）後に次のウェーブを自動開始する。
- 到達ウェーブの永続化のため`src/logic/progress.ts`を新設（`loadBestWave`/`saveBestWave`、ロードアウトのセーブとは別のlocalStorageキー）。ゲームオーバー時に`saveBestWave`を呼び、`restartText`に「到達: Wave N　ベスト: Wave M」を表示する。連打的な多重保存を避けるため`gameOverHandled`フラグでゲームオーバー処理を1回だけ実行するようにした。
- HUDのSCOREテキストに「WAVE N」を追記し、TIPSオーバーレイの文言も「ウェーブが進むほど敵の数・体力・防御力が上がっていきます。どこまで生き残れるか挑戦！」に更新。ルートハブページ（`index.html`）の剣戟の森の説明文も「ゴールを目指す」から「ウェーブごとに強くなる敵の群れを迎え撃ち、どこまで生き残れるかに挑む」に変更した。
- Vitest（side-scroller、既存119件+新規10件=129件）は全て成功。`npm run typecheck`/`npm run lint`/`npm run build`実行済み、全て成功。Playwrightで実際にウェーブ1をクリアし、WAVE表示が2に切り替わって新しい敵がスポーンすることを確認した。
