# TODO

- [x] ジャンル決定: 放置・育成系「ポーション工房」／販路: CrazyGames + 日本向け無料公開
- [x] Phaser + TS 雛形、経済ロジックMVP、i18n（ja/en）、CrazyGames SDK ラッパー
- [x] 転生（プレステージ）システム
- [x] 見た目強化（浮遊テキスト・泡・購入演出・転生フラッシュ）
- [x] GitHub Pages デプロイ設定・公開確認済み（https://b-w-hiroki.github.io/AI_project002/potion-workshop/ ／リポジトリ再編に伴い `games/potion-workshop/` へ移動）
- [x] 実績システム（10種）
- [x] 設備の追加ティア（5→8種）、クリック強化アップグレード
- [x] サウンド（Web Audio合成、ON/OFF切替）
- [x] オフライン進行モーダル
- [x] モバイル対応（Scale.FIT、タッチ操作、safe-area対応）
- [x] バンドルサイズ最適化（Phaserをvendorチャンクに分離）
- [x] セーブのエクスポート/インポート（JSON）
- [x] 軽量アナリティクス（ローカル集計のみ、外部送信なし）
- [x] CI強化（ESLint導入、Actionsに組み込み）
- [x] OGP/メタタグ整備（プレースホルダー画像は要差し替え）
- [x] CrazyGames/PLiCy投稿準備資料（docs/submission.md）
- [ ] CrazyGames に開発者登録して投稿（人間の作業: アカウント作成）
- [ ] PLiCy 投稿（人間の作業: アカウント作成）
- [ ] OGP画像を単色プレースホルダーから差し替え（任意）
- [x] クリック強化の一括購入（x1/x5/x10/x100/MAX 選択、資金内で買えるだけ買う部分約定）
- [x] オフライン進行上限の拡張（基本12h→最大72h、ソース加算式の柔軟な仕組み。現状は essence 購入型の拡張1種のみ実装、他ソースは`setOfflineCapBonus`/`addOfflineCapBonus`で追加可能）
- [x] UIのビジュアル刷新（`src/ui/theme.ts`新設）: グラデーション背景、角丸パネル/ボタン（`makeRoundedRect`/`drawPanel`）、ブリューボタンの二重リング演出、モーダルの角丸化。既存の`.setFillStyle()`ベースの購入可否表示ロジックはそのまま流用できるよう`RoundedRect`型に`setFillStyle`互換メソッドを持たせた設計にした
- [x] UI/UXの洗練（タイポグラフィ/マイクロインタラクション）: `theme.ts`に`TYPE`タイポグラフィスケール（h1/h2/numeric/body/small、字間・太さを統一）を追加。`makeRoundedRect`のホバー演出を内部状態化（`baseColor`/`hovering`を保持し常に合成して描画することで、`refreshUI()`の毎フレーム`setFillStyle`呼び出しでホバー色が消えてしまう問題を解消）。ポーション表示に`SmoothedCounter`（指数平滑化）、エッセンス表示に`popOnChange`（値変化時だけ軽くポップするテキスト演出）を導入
- [x] 階層（エレベーション）デザインシステム: ユーザーから「まだ全然いけてない、ベテランデザイナー目線でチューニングして」との率直なフィードバックを受け実施。`theme.ts`に`ELEVATION`（背景/ゾーンパネル/カードの3段階の明度トークン）と`SPACE`（4/8/16/24/32pxの余白スケール）を追加。ブリューエリア・設備リストをそれぞれ1枚のゾーンパネル（`ELEVATION.zone`）で囲み、背景と同化していた階層を明確化。タイトルの🧪絵文字はヘッドレスChromium環境で豆腐化（空の四角）することが確認できたため、`drawFlaskIcon`でGraphics描画のオリジナルアイコンに置き換え、環境依存の表示崩れリスクを解消した
- [x] デザインレビューでの枠の重なり修正: サウンドボタンの🔊/🔇絵文字も同環境で判読不能な塊に潰れることが判明し`drawSpeakerIcon`に置き換え。転生ボタンとゾーンパネル下端の余白不足（8px→拡張）を修正
- [x] データ可視化・フッター活用: `theme.ts`に`drawProgressBar`（トラック+フィルの2層Graphics、`setRatio`で軽量更新）を追加。転生カードに「累計醸造数/転生解放ライン」の進捗バーを表示（解放後は非表示）。画面下部の空白（ゾーンパネル下端〜キャンバス下端）をフッターパネル化し、これまで未表示だった生涯統計（累計醸造・実績数・転生回数・プレイ時間）を表示。Export/Importボタンもフッター内に統合
- [x] 装飾・演出の強化: 転生ボタンが解放された瞬間だけパルスするグロー（`prestigeWasAffordable`で発生を1回だけ検知しtweenを開始/停止、毎フレームのrefreshUIからtween.addを連打しない設計）を追加。設備一覧の各行に色相をずらしたジェム風アイコンを表示し、8種の設備を一目で見分けやすくした
- [x] 世界観の刷新（淡い青空・ファンタジー系）: ユーザーから「色味などの世界観もっと明るく、ソシャゲ調のビジュアルにしたい」との要望を受け、AskUserQuestionで「淡い青空・ファンタジー系」を選択。`THEME`/`ELEVATION`を濃紺ベースから水色〜白グラデーション基調のパステル配色に刷新（`panelFill: 0xf3f9ff`、`ELEVATION.bg: 0xcfe9ff`等）。パネルが明るくなった分、本文テキストは濃紺〜紺青系（`textPrimary: #2d3a52`）に統一し可読性を確保。`makeRoundedRect`のホバー演出（白方向ブレンド→水色方向ブレンドに変更）、`drawPanel`のガラス風ハイライト・影・デフォルト枠色も明るい配色向けに調整
- [x] UIのスッキリ化: ユーザーから「UIをもっとスッキリ」との要望を受け、設備リストの1行に詰め込まれていた「名前 ×所持数 / Cost: X +Y/sec」のテキストを分離。名前+所持数を主役、生産量を小さく補足、コストは右側に「値札」として独立させ、ショップの商品行らしいレイアウトにした
- [x] 枠の重なり修正（デザインレビュー）: スクリーンショットを目視精査し、(1)サウンドボタンの🔊/🔇絵文字も同様に環境依存で潰れて表示されることを発見し`drawSpeakerIcon`のオリジナル描画に置き換え、(2)転生ボタンとゾーンパネル下端の余白がほぼゼロ(8px)だったためパネルを拡張して是正

## 2本目: 剣戟の森（横スクロールアクション）

- [x] 雛形作成（`games/side-scroller/`）: プレイヤー移動・ジャンプ・剣攻撃、敵（徘徊AI・接触ダメージ）、HP/スコアHUD、ゴール判定
- [x] 戦闘ロジックを純粋関数化（`src/logic/combat.ts`）、Vitest 20件
- [x] GitHub Pages デプロイ設定（`/side-scroller/` サブパス、ルートと同時ビルド）
- [x] キャラクター/UIデザイン強化: 単色矩形→頭・胴・剣を持つ簡易ヒューマノイドスプライトに変更、HUDにパネル背景、リザルト画面にパネル演出
- [x] 当たり判定の調整・修正:
  - プレイヤーと敵が overlap のみで衝突判定（collider）が無く、敵の体をすり抜けてしまい間合いに留まれないのが「当たらない」と感じる主因だった → collider に変更し、ぶつかって自然に隣合うように修正
  - 攻撃レンジを 46→60px に拡大、密着時に敵中心がわずかに背後判定になっても取りこぼさないよう背後方向に12pxの許容を追加
  - 異なる高さの足場にいる敵を誤って巻き込まないよう縦方向の判定ガードを追加
- [x] 被弾/命中フィードバック強化（敵ヒット時のフラッシュ・ノックバック、被弾時のプレイヤー点滅、カメラシェイク）
- [x] リトライ導線（GAME OVER/CLEAR 時に R キーでシーン再スタート）
- [x] 操作TIPS（Enterキーで操作方法オーバーレイの開閉）
- [x] 武器種別（近接/中距離/遠距離を1/2/3キーで即時切替。全種類を最初から所持、装備取得システムは未実装）
- [x] スキル（Cキーで即時発動、独自クールダウン。ダッシュ薙ぎ払いの1種のみ実装）
- [x] 奥義（必殺ゲージ、通常/スキル命中で+10%ずつ加算。ゲージ満タン時に↓→X の格闘ゲーム風コマンド入力で発動、広範囲攻撃）
- [x] 秘奥義（スコア500到達で解放。解放後はゲージ満タン時に↓→↓→X の追加コマンドで発動、奥義よりさらに広範囲・高倍率）
- [x] コマンド入力認識を`src/logic/commandInput.ts`に純粋関数として分離、Vitest 7件
- [ ] レベルデザインの拡充（現状は敵8体・浮遊足場6個の1ステージのみ）
- [ ] 効果音・BGM（ポーション工房と同様 Web Audio 合成を想定）
- [ ] タイトル画面（現状いきなりプレイ開始）
- [x] モバイル対応（タッチ操作）: `src/ui/touch.ts`に共通ヘルパー（`makeTappable`/`bindHeldKey`/`bindVirtualJoystick`/`isTouchDevice`/`buildOrientationWarning`）を新設。`bindHeldKey`/`bindVirtualJoystick`はKey#onDown/onUpを直接呼ぶことで既存のキーボード判定ロジック（`handleMovement`/`handleAttack`等）を一切変更せずタッチ入力を統合。タッチデバイス検知時のみ`GameScene`に左半分の動的仮想スティック（触れた場所を中心に現れ指の動きに追従し離すと消える）＋右下に攻撃/スキル/武器切替/アイテム使用/TIPSの仮想ボタンを表示、`LoadoutScene`の各行もタップ判定を広げた。縦向き時は両画面で横向きを促すオーバーレイを表示。コマンド入力（↓→X等）はスティックのJustDown経由で自動的に動作
  - 当初は固定表示の4方向D-padボタンだったが、ユーザーから「もっと今風にできる？表示しないとか」との要望を受け、常時表示のD-padから動的仮想スティック（Fortnite Mobile等のモバイルアクションゲームで一般的な、非表示→タッチ地点に出現→追従→離すと消える方式）に置き換えた
  - 実装中に発見・修正したバグ: Key#onUpはisDown/_justDownを無条件でリセットするため、タップ操作でtouchstart→touchendが同一フレーム内（Scene#updateより前のPRE_STEPで入力処理される）に処理されると、瞬間タップの入力（武器切替やアイテム使用等のJustDown系操作）が消えてしまう事象があった。`scene.time.delayedCall(0,...)`では同じフレーム内でClockが処理されてしまい対策にならず、ブラウザ標準の`requestAnimationFrame`で次の描画フレームまで確実に遅延させて解消
  - 既存コードの抜けも合わせて修正: TIPSオーバーレイの背景タップで閉じられなかった／ゲームオーバー・クリア画面のリトライがタップできなかった（いずれもキーボードのみ対応だった）
  - Vitest対象外（Phaser/DOM依存のUI）だが、Playwrightのタッチエミュレーション（`hasTouch: true`）で状態1〜5すべてを一通り確認済み
- [ ] モバイル対応の残課題: 実機（iOS Safari/Android Chrome）での確認、ダブルタップズーム抑止の実機検証、ゲームオーバー/クリア画面表示中も仮想ボタンが視覚的に残ってしまう点の整理（機能的には無害だが見た目の整理余地あり）
- [x] UIのビジュアル刷新（`src/ui/theme.ts`新設）: グラデーション背景＋放射グロー、角丸パネル（`drawPanel`）、ホバー/押下フィードバック付きボタン（`makeButton`、Key#onDown/onUp橋渡し方式とは独立）。HUDパネル・TIPS/召喚媒体/ステージバフの各オーバーレイ・LoadoutSceneの全パネルに適用
- [x] UI/UXの洗練（タイポグラフィ/マイクロインタラクション）: ポーション工房と同じ`TYPE`タイポグラフィスケール・`popOnChange`ヘルパーを`theme.ts`に追加し共通化。HUDのHP/スコア/武器/スキル/コンボ/アイテムテキストに適用し、武器切替テキストは`popOnChange`で切替時にポップさせて気付きやすくした
- [x] 階層（エレベーション）デザインシステム: ポーション工房と共通の`ELEVATION`/`SPACE`トークンを`theme.ts`に追加。LoadoutSceneの武器一覧/宝箱・鍛治/防具の各ゾーンパネルを`fillAlpha 0.55`→`ELEVATION.zone`色×`0.92`に変更し、背景と同化していたパネルの視認性を改善（既存の`RARITY_COLOR`によるレアリティ色分けは維持）
- [x] デザインレビューでの枠の重なり修正: 近/中/遠距離スロットパネルと武器一覧ゾーンパネルの隙間ゼロ接触（10px確保）、「宝箱を開ける」ボタンがゾーンパネル上端からはみ出していた問題（パネル・ボタン位置を再計算し完全内包）を修正
- [x] データ可視化: LoadoutSceneの武器一覧見出し下に、所持武器のレアリティ内訳（N/R/SR/SSR/UR）を`RARITY_COLOR`で色分けした積み上げバーとして表示（`renderRarityBar`、`refresh()`で武器取得のたびに再計算）
- [x] 装飾・演出の強化: 近/中/遠距離スロットが未設定の間、パネル外周をゆっくりパルスさせるグローを重ねて空きスロットに気付きやすくした。武器一覧でSR以上のレアリティに★（SR=★/SSR=★★/UR=★★★）を表示し、ソシャゲ的なレアリティ演出を追加
- [x] 世界観の刷新（淡い青空・ファンタジー系）: ポーション工房と共通の`THEME`/`ELEVATION`刷新に加え、`main.ts`のキャンバス背景色とGameSceneの空（`buildLevel`の全面矩形→Graphicsグラデーション）を濃紺から淡い青空に変更。地面も暗い紫がかった色から茶色い土＋緑の芝に、浮遊床は白い「雲」風の見た目に変更してファンタジー感を強めた。HUD/TIPS/召喚媒体/ステージバフの各オーバーレイのパネル・テキスト色を明るい配色向けに再調整し、`spawnFloatingText`には白フチ（`stroke`）を追加して明るい背景上でも読めるようにした
- [x] UIのスッキリ化＋ゲームらしい見た目: ユーザーから「UIをもっとスッキリ、あとから差し替える前提でもっとゲームらしい見た目にしたい」との要望を受け、AskUserQuestionで「武器一覧のカード型グリッド化」「ポーション工房の設備リスト情報量削減」の両方が選択された。LoadoutSceneの武器一覧を、1行に全ステータスを詰め込んだテキストリスト（3列×3行のカード型グリッド、`INVENTORY_GRID`定数で列数/カードサイズ/最大行数を管理）に刷新。各カードは`drawWeaponKindIcon`（近距離=剣/中距離=槍/遠距離=弓のシルエットをGraphics描画、本物の武器アイコン素材に差し替える前提のプレースホルダー）とレアリティ色の枠・★演出・名前のみのミニマル表示にし、詳細ステータス（距離/力/速度/範囲/重さ/コンボ）は選択時に`inventoryDetailText`へまとめて表示する方式にした。9件を超える所持武器は「他N件」表示（オーバーフロー時に黙って切り捨てない）
- [x] 枠の重なり修正（デザインレビュー）: (1)近/中/遠距離スロットパネルの下辺と武器一覧ゾーンパネルの上辺が隙間ゼロで接触していたのを10px空けて是正、(2)「宝箱を開ける」ボタンが自身のゾーンパネル上端から8pxはみ出していたのをパネルサイズとボタン位置を調整して完全に内包させた
- [ ] OGP/メタタグ整備（ポーション工房と同様の対応が必要）
- [ ] 武器の取得/所持システム（現状は3種類とも最初から使用可能）
- [ ] スキル・奥義の種類追加（現状はそれぞれ1種類のみ）
- [x] 武器パラメータ体系のロジック層を実装（`src/logic/loadout.ts`）: レア度(N/R/SR/SSR/UR)、WeaponTemplate×6（各kind2種）、WeaponInstance（強化/進化/熟練度）、宝箱/鍛治入手、基本装備、インゲーム内重複強化（RunWeaponState、入手+強化2回+アルティメット化1回の最大4段階）、防具/アイテム消耗品、ロードアウト＋localStorage永続化。Vitest 34件追加（計80件）
- [x] アウトゲーム画面（`LoadoutScene`）を実装: 所持武器一覧、宝箱/鍛治での入手、近/中/遠スロットへの割当（種別不一致は弾く）、ステージ開始でGameSceneへロードアウトを渡す。`main.ts`の起動シーンをLoadoutScene→GameSceneに変更
- [x] GameScene側でのロードアウト反映: `combat.ts`に`customWeapons`（装備スロットごとの実効武器定義の上書き）を追加し、`setCustomWeapon`/`clearCustomWeapon`で`currentWeapon`の参照先を差し替え可能にした。ステージ開始時は`baseEquipmentStats`ベースの基本装備からスタートし、召喚媒体ピックアップで`resolveSummon`→`toWeaponDef`を通じてロードアウト武器へ切り替わる
- [x] 召喚媒体のインゲーム実装: 拾うと`this.physics.pause()`で一時停止し、ヴァンサバ風の選択UI（近/中/遠を1/2/3キーまたはクリックで選択、`docs/decisions.md`の設計通り「アウトゲーム設定したものの中から選ぶ」方式）を表示。選択すると空中から武器が降ってきてプレイヤーが受け取る演出（Tweenで落下→フラッシュ→プレイヤーが一瞬拡大）を再生。同じ個体を再度召喚すると`resolveSummon`が自動的に`stackRunWeapon`を呼びインゲーム内重複強化が進む
- [x] 敵の防御力パラメータ（`EnemyState.defense`、`damageEnemy`で連続ヒットごとに`DEFENSE_SHRED_PER_HIT`ずつ削れる）、無被弾スーパーコンボ倍率（`comboStreak`/`superComboMultiplier`、10コンボ→×1.1、30コンボ→×1.2、被弾でリセット）を実装し、`GameScene.applyHit`で敵への実ダメージに反映
- [x] 防具/アイテム消耗品のインゲーム実装（拡張版）: 防具ピックアップで`armorCharges`を加算（`damagePlayer`がHPより先に耐久を消費）。アイテムは複数種類（ポーション/剛力の護符/俊足の護符）を所持でき、拾うと所持数が増え、Z/V/Bキーでそれぞれ使用する（HUDに所持数を常時表示）
- [x] アイテムの所持一覧UI・複数種のアイテム: `ITEM_DEFS`に3種類を定義し、HUDの`itemsText`に「Z:ポーション×N V:剛力の護符×N B:俊足の護符×N」を表示。所持していない状態でキーを押すとヒントメッセージを表示するガードも実装
- [x] 防具のアウトゲームでの事前設定・ロードアウト: `ARMOR_TEMPLATES`（なし/革の鎧/鎖帷子/板金鎧、Tierごとに耐久とコストが増加）を追加し、`LoadoutScene`で出撃のたびに1つ選択・購入する方式にした（武器のようなレア度ロール/所持在庫は持たせず、選択式のみ）。ステージ開始時に選択Tierの耐久が`armorCharges`として付与される
- [x] ステージ側から提示される一時バフ: `STAGE_BUFF_POOL`（攻撃力アップ/俊足/HP自動回復）を新設し、専用のピックアップ「stageBuff」を拾うとヴァンサバ風の一時停止選択UI（`rollStageBuffOptions`でランダム3択）が開く。アウトゲームのロードアウトとは完全に独立したプール。攻撃力/俊足バフはアイテムの護符と共通の`combat.ts`の`buffs`機構（`applyBuff`/`isBuffActive`/`buffDamageMultiplier`/`buffSpeedMultiplier`）を再利用し、HP自動回復は`applyRegen`/`tickRegen`で実装
- [ ] 【未着手】アイテム/防具のレア度・強化システム（現状これらは武器と異なり個体差やレア度を持たない、シンプルな種別のみ）
- [ ] 【未着手】召喚媒体を失う演出（現状は一度召喚した武器はステージ中ずっと保持する設計のまま。ユーザー合意通りなので変更は不要だが、将来的な難易度調整の余地として記載）

## ルートハブページ

- [x] リポジトリ直下のハブページ（`index.html`）をリッチなカードUIに刷新。データ駆動（`PROJECTS`配列）、外部リポジトリ制作物へのリンク集対応
- [x] コピーをゲーム限定表現から一般化（「ゲーム一覧」→「制作物一覧」、今後Webアプリ等も掲載する前提の文言に変更）
- [ ] デザイン参考: https://amix-design.com/tl/web-g-games/ （ユーザー共有。作り込みすぎる必要はないが、情報整理の参考にする）
